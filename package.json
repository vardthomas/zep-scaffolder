{
  "name": "zep-scaffolder",
  "version": "1.0.0",
  "description": "Generates React Components for smart contracts",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-ts": "rm -rf lib && tsc --jsx react"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^16.8.6",
    "rimble-ui": "^0.9.3",
    "styled-components": "^4.3.2"
  }
}
